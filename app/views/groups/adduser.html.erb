<h1>Adding user</h1>

  <%= form_tag({controller: "groups", action: "updateuser"}, id: @group.id, method: :patch) do %> 
    <%= render "/shared/error", model: @group %>
  
      <%= email_field_tag (:search_user_email) %>
      <%= submit_tag 'Add User', name: nil %>
  <% end %>


<%= link_to 'Show', @group %> |
<%= link_to 'Back', groups_path %>
